import { defineAdditionalConfig } from 'vitepress'

export default defineAdditionalConfig({
  themeConfig: {
    // Default sidebar is described in the main config .vitepress/config.mts
    //sidebar: []
  }
})
